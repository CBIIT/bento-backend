# use cicd-microservice container
image: cbiitssrepo/cicd-microservice:latest

build:
  stage: build
  variables:
    NODE_OPTIONS: "--max_old_space_size=4096"
  script: ansible-playbook -vvv devops/ansible/bento-build.yml -i devops/ansible/hosts