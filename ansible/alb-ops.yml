---
- name: Add/Remove alb rules
  hosts: docker-local
  connection: local
  gather_facts: yes
    
  tasks:
    - name: remove alb rules
      include_role:
        name: alb-ops
        tasks_from: remove-alb-rules