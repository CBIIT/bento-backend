---
- name: cloudone-ecs cicd pipeline
  hosts: cloudone
  connection: local
  gather_facts: no
    
  tasks:
    - name: This is a build stage
      include_role:
        name: cloudone-ecs
        tasks_from: build