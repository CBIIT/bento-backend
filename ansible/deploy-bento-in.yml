---
- name: deploy stage of bento pipeline
  hosts: bento
  gather_facts: yes
  become: yes
    
  tasks:
    - name: deploy bento-icdc
      include_role:
        name: bento-in
        tasks_from: deploy
  