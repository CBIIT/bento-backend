---
- name: Redis stage of icdc pipeline
  hosts: icdc
  connection: local
  gather_facts: no
    
  tasks:
    - name: flush redis cache
      include_role:
        name: redis
        tasks_from: redis_icdc
        
      