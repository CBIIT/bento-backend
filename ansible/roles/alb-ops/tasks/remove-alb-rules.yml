---

- name: gather info about alb
  elb_application_lb_info:
    names: 
      - bento-alb-{{tier}}
    region: "{{region}}"

  register: alb_info

- debug:
    msg: >
     "{{alb_info.load_balancers[0].listeners[0].certificates[0].certificate_arn}}"

- name: set facts
  set_fact:
    subnets: "{{ alb_info.load_balancers[0].availability_zones| json_query('[*].subnet_id')}}"
    alb_sg: "{{ alb_info.load_balancers[0].security_groups }}"

- debug: 
    msg: >
     "{{subnets}}"
     "{{alb_sg}}"

# - name: modify application load balancer
#   elb_application_lb:
#     name: "{{ application_load_balancer.name }}"
#     security_groups: "{{ alb_sg }}"
#     subnets: "{{ subnet_id_list }}"
#     listeners: "{{ application_load_balancer.listeners }}"
#     deletion_protection: yes
#     state: present

# - name: Creates a new set of listeners rules for the ALB
#   elb_application_lb:
#     name: '{{ alb_name }}'
#     state: present
#     region: '{{ region }}'
#     security_groups: '{{ alb_security_groups }}'
#     subnets: '{{ alb_subnets }}'
#     purge_listeners: '{{ should_purge_listeners }}'
#     deletion_protection: yes
#     listeners: '{{ listeners }}'
#   register: alb_with_new_listeners









# - debug:
#     msg: >
#      "{{ subnets}}"
#      "{{alb_sg}}"