---
# vars file for alb-ops
tier: "{{ lookup('env','TIER') }}"
workspace: "{{ lookup('env','WORKSPACE') }}"
# project: "{{ lookup('env','PROJECT') }}"
# action: "{{ lookup('env','ACTION') }}"
region: us-east-1
# alb_name: "{{project}}.{{tier}}.value"
alb:
  - icdc:
      dev: DEV-A-Appli-Caninedata-8UHLKJYN
      qa: ""
      stage: ""
      prod: ""
  - ctdc:
      dev: "test"

