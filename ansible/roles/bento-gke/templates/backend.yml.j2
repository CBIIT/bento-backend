kind: Service
apiVersion: v1
metadata:
  name: bento-backend-service-{{tier}}
spec:
  type: NodePort
  selector:
    app: bento
    role: backend
    env: {{tier}}
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http