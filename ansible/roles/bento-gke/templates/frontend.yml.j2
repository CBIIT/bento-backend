kind: Service
apiVersion: v1
metadata:
  name: bento-frontend-{{tier}}
  namespace: {{tier}}
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: bento
    role: frontend