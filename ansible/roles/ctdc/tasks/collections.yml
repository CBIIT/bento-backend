---
- name: install aws collection
  command: ansible-galaxy collection install {{item}}
  loop:
    -  amazon.aws
    -  community.aws
    -  git+https://github.com/CBIIT/icdc-devops.git#/ansible/ansible_collections/bento/common/,ansible_collections