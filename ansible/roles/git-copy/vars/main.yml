---
# vars file for roles/git-tag
git_date: "{{ lookup('pipe','date +%Y%m%d%H%M') }}"
username: "{{ lookup('env','GIT_USERNAME') }}"
password: "{{ lookup('env','GIT_PASSWORD') }}"
copy_from: prep-prod
copy_to: prod
git_home: /tmp/work
git_base: https://{{username}}:{{password}}@github.com/CBIIT
