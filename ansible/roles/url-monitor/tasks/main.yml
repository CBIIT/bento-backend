---
# tasks file for url-monitor
- name: "check {{app_name | default(default_app_name)}} for availability"
  uri:
   url: "{{item | default(default_url)}}"
   follow_redirects: none
   method: GET
  register: _result
  until: 'status' in _result
  retries: 100
  delay: 10
  register: output

- name: show output
  debug:
    msg: "{{output}}"
