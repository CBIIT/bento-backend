version: '3'
services:
  processor:
    image: comets/processor
    container_name: processor
    volumes:
      - "{{app_base_directory}}/logs:/deploy/logs"
    restart: always
    ports:
      - "61613:61613"