ARG ECR_REPO
ARG PROJECT

RUN echo ${PROJECT}

FROM ${ECR_REPO}/${PROJECT}-backend:release
MAINTAINER icdc devops team

RUN rm -rf /usr/local/tomcat/webapps/ROOT
COPY target/ROOT.war /usr/local/tomcat/webapps/
