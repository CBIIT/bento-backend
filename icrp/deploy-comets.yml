---
- name: setup ecs agent
  hosts: all
  connection: local
  become: yes
  gather_facts: yes

  tasks:
    - name: Run tasks/other.yaml instead of 'main'
      include_role:
        name: deploy-comets
        tasks_from: build