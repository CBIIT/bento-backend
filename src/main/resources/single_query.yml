# Filter Type
# - default
# - table

# Return Type
# - default

query:
  - name: subjectOverview
    index: subjects
    filterType:
      type: table
      selectedField: subject_id_num
    resultType: default
  - name: sampleOverview
    index: samples
    filterType:
      type: table
      selectedField: sample_id_num
    resultType: default
  - name: fileOverview
    index: files
    filterType:
      type: table
      selectedField: file_name
    resultType: default
  - name: fileIDsFromList
    index: files
    filterType:
      type: default
      selectedField: file_id
    # List<String>
    resultType: str_list
  - name: filesInList
    index: files
    filterType:
      type: table
      selectedField: file_name
    resultType: default
  - name: findSubjectIdsInList
    index: subjects
    filterType:
      type: default
#      size: 10000
    resultType: default
